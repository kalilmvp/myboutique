server:
  port: 8222

eureka:
  client:
    fetchRegistry: true

zuul:
  routes:
    order:
      path: /order/**
      serviceId: order-service
    product:
      path: /product/**
      serviceId: product-service
    customer:
      path: /customer/**
      serviceId: customer-service
