server:
  port: 8340

#eureka:
#  instance:
#    hostname: ${EUREKA_HOST}

zuul:
  routes:
    order:
      path: /order/**
      serviceId: order-service
    product:
      path: /product/**
      serviceId: product-service
    customer:
      path: /customer/**
      serviceId: customer-service
